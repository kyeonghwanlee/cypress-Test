#!/usr/bin/env node
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const logging_1 = require("@app-config/logging");
const encryption_1 = require("@app-config/encryption");
(0, encryption_1.startAgent)().catch((error) => {
    if (error instanceof Error) {
        logging_1.logger.error(`Error: ${error === null || error === void 0 ? void 0 : error.toString()}`);
    }
    else {
        logging_1.logger.error('An unknown error occurred');
    }
    process.exit(1);
});
//# sourceMappingURL=start-secret-agent.js.map